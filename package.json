{
  "name": "dco",
  "private": true,
  "version": "1.0.0",
  "description": "a GitHub App that enforces the DCO on Pull Requests",
  "author": "Brandon Keepers",
  "license": "ISC",
  "repository": "github:dcoapp/app",
  "scripts": {
    "start": "probot run ./index.js",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js --coverage",
    "posttest": "npm run lint",
    "lint": "biome check --linter-enabled=false --assist-enabled=false .",
    "lint:fix": "biome format --write ."
  },
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "@sentry/node": "^10.63.0",
    "email-validator": "^2.0.3",
    "probot": "^14.3.2"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.2",
    "jest": "^30.4.2",
    "nock": "^14.0.0",
    "smee-client": "^1.0.1"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "jest": {
    "testEnvironment": "node",
    "coverageThreshold": {
      "global": {
        "statements": 100,
        "branches": 100,
        "functions": 100,
        "lines": 100
      }
    },
    "snapshotFormat": {
      "escapeString": true,
      "printBasicPrototype": true
    }
  },
  "release": {
    "branches": [
      "main"
    ]
  }
}
